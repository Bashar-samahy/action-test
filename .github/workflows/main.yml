name: Student project backup

on:
  schedule:
    - cron: '0 * * * *'

jobs: 
    backup:
        runs-on: ubuntu-latest
        steps: 
            - name: Backup 
              run: |
                echo "backing up project at $(date)"
                echo "backup created"